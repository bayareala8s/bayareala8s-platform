# BayAreaLa8s Platform (Skeleton)

Production-grade starter for BayServe v2 on AWS.

Includes:
- React + Vite + TypeScript frontend (S3 + CloudFront ready)
- Node.js Lambda backend (API Gateway + Lambda)
- Terraform IaC skeleton for prod environment
